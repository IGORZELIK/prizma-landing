---
interface Props {
  t: Record<string, any>;
}
const { t } = Astro.props;

const stats = t.lang === 'ru' ? [
  { value: 1, label: 'Источник. Одна идея.' },
  { value: 6, label: 'Форматов контента' },
  { value: 24, label: 'Автоматизация 24/7' },
  { value: 85, label: '% Экономии времени' },
] : t.lang === 'de' ? [
  { value: 1, label: 'Quelle. Eine Idee.' },
  { value: 6, label: 'Content-Formate' },
  { value: 24, label: 'Automatisierung 24/7' },
  { value: 85, label: '% Zeitersparnis' },
] : [
  { value: 1, label: 'Source. One idea.' },
  { value: 6, label: 'Content formats' },
  { value: 24, label: 'Automation 24/7' },
  { value: 85, label: '% Time saved' },
];
---

<section class="stats-bar">
  {stats.map(stat => (
    <div class="stat-item reveal">
      <div class="stat-value gradient-text text-unbounded" data-count={stat.value}>0</div>
      <div class="stat-label">{stat.label}</div>
    </div>
  ))}
</section>

<style>
  .stats-bar {
    position: relative;
    z-index: 1;
    display: flex;
    justify-content: center;
    gap: 80px;
    padding: 80px 40px;
    border-top: 1px solid var(--border-glass);
    border-bottom: 1px solid var(--border-glass);
  }

  .stat-item {
    text-align: center;
  }

  .stat-value {
    font-size: 2.5rem;
    font-weight: 700;
  }

  .stat-label {
    font-size: 0.85rem;
    color: var(--text-dim);
    margin-top: 8px;
    letter-spacing: 1px;
  }

  @media (max-width: 768px) {
    .stats-bar {
      flex-wrap: wrap;
      gap: 40px;
    }
    .stat-item {
      flex: 0 0 40%;
    }
  }

  @media (max-width: 480px) {
    .stats-bar {
      flex-direction: column;
      align-items: center;
      gap: 32px;
    }
  }
</style>
